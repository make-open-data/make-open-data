name: 'makeopendata'
version: '1.0.0'
config-version: 2

profile: 'makeopendata'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"

models:
 prep:
  demographie:
    +tags: demographie
  geo:
    +tags: geo
  habitat:
    +tags: habitat
  immo:
    +tags: immo
  